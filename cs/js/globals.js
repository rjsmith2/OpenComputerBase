// place globals here

FIREFOX = (navigator.userAgent.toString().toLowerCase().indexOf("firefox") != -1);
CHROME = (navigator.userAgent.toString().toLowerCase().indexOf("chrome") != -1);
QUEUED = []; // used for queueDom function